import React from 'react';

function ResultExamRoomPage({
    roomId,
}: {
   roomId: string;
}) {
    console.log(roomId);
    return (
        <div>
            
        </div>
    );
}

export default ResultExamRoomPage;